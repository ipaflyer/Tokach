# Толкач — спецификация прототипа v0.2

Прототип проверяет: интересна ли развилка «забрать тон / переписать тон» сама по себе.

Сессия 10–20 минут. Два игрока, одно устройство. Без сюжета, прокачки, полировки.

## Системы

1. **Поле.** −6…+6. Старт со **смещением 1 к первому** на камне (A начинает на −1; если первый B — на +1). A толкает в минус, B в плюс. Победа камня: `< -6` / `> +6`.

2. **Тон.**
   - **`opening`** (первый ход камня): сдвиг = карта, тон = карта, **закрыт**.
   - На закрытом тоне нельзя сыграть то же число, если в руке есть другое. Если карта одна — можно (unlock через `rewrite`).
   - **`setup`** (тон молчал, не opening): сдвиг = карта, тон = карта, **открыт**.
   - **`steal`** (карта = открытый тон): сдвиг = карта×2 (дефолт), тон сгорает.
   - **`rewrite`**: сдвиг = карта, тон = карта, открыт.

3. **Рука 1–4.** Открыта. Сброс. Рефил из пустой руки.

Калибровки резонанса до первого хода сессии: `double` (×2, по умолчанию), `plus1`, `plus2`.

## Ход

Выбор карты → расчёт выше → сдвиг → сброс/рефил → проверка края → смена игрока (если камень жив).

Между камнями: руки 1–4, сбросы пусты, тон молчит, снова `opening` и задел первому. Первые ходы камней: A, B, A, B, A.

## Зачем так (v0.2)

v0.1 ломался: `setup→steal×2` при зеркальных руках давал второму чистый плюс; «всегда кради» → 100% камней второму.

- Closing только на `opening` (не на каждый setup) — иначе жадные циклы на рефилах.
- Задел +1 первому + ×2 — по симуляции humanish победы первого на камне в коридоре ~35–65%.
- `plus1` как дефолт отвергнут: партии длинные, первый почти не выигрывает.

## Первые 30 секунд

Поле, камень уже на −1 (или +1), «тон: молчит», руки 1–4. Opening: сила + закрытый тон. Второй обязан переписать другим числом. Затем первый решает: красть или нет.

## Решение, которое повторяется

После того как тон открыт: **забрать или переписать**.

## Сессия

5 камней всегда. Победитель — больше камней. Длительность 10–20 мин — рамка плейтеста.

## Лог

На ход: как в v0.1, тип ∈ {`opening`,`setup`,`steal`,`rewrite`}, плюс факт закрытого тона виден в `toneBefore`/`toneAfter` и запрете совпадения.

На камень/сессию — агрегаты и заметки плейтеста (форма в UI).

## Как читать лог

Жива: есть `мог украсть, не украл`; спор о ходе; 5-й камень не шаблон; нет 3:2 автомата из одного стиля.

Мертва: шаблон с 3-го камня; просят системы без спора о ходе; opening/задел сделали старт пресным.

## Противоречие

Замок и задел — не «физика толкача», а анти-эксплойт. Живой плейтест решает, терпимо ли это для фантазии.
